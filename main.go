package main

import "fmt"

func main() {
	var a = Graph{make(map[int][]int)}
	a.AddEdge(1, 2, false)
	a.AddEdge(1, 4, false)
	a.AddEdge(2, 3, false)
	for 
	if HasEdge(&a, 1, 2) {

	}
	fmt.Println("Hello world!")
}
